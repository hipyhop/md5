# DONE
--------------------
# require Digest::MD5
# MD5 single string test 
# MD5 single file test 
# --output file creates 'checksums.md5' 
# 'checksums.md5' content is correct
# basic --format test

# TODO
-------------------
check --output prompts for overwite if 'checksums.md5' exists
check --out_file creates correct file
check --verify correctly verifies file checksums using standard format
implement and test --verify correctly verifies file checksums using custom format
implement and test --verify correctly verifies file checksums using custom format
more advanced format output tests (??)
test checksumming input strings from STIDN
test checksumming input files from STIDN
test recursive directory checksumming correctly recurses, no duplicated

